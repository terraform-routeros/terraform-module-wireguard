on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: "ubuntu-latest"
    steps:
      - uses: codfish/semantic-release-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}